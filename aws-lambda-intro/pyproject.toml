[project]
name = "aws-lambda-intro"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.38.18",
    "moto>=5.1.4",
    "pytest>=8.3.5",
    "pytest-cov>=6.1.1",
]
